// Placeholder — full user management UI (list/create/edit/deactivate users,
// permission + store_access checkboxes) is built in Task 10. This stub exists
// so Settings.jsx's lazy import resolves at build time (Vite/rolldown needs
// the module to exist in the graph even for a code-split dynamic import).
export default function UsersManager() {
  return <div className="users-manager">User management coming soon.</div>;
}
